# eDataDRF

Basic version of data format, (controlled) vocabulary, and standardised export functions for STOP's eData format. Used in the Shiny app and Copper AEP. Not versioned very well, or particularly consistent, because I'm in a hurry, but at least it passes check()?